apiVersion: v1
kind: Service
metadata:
  name: clawbuddy-llm-proxy
  namespace: clawbuddy-system
  labels:
    app: clawbuddy-llm-proxy
spec:
  type: ClusterIP
  selector:
    app: clawbuddy-llm-proxy
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP

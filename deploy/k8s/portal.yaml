apiVersion: apps/v1
kind: Deployment
metadata:
  name: clawbuddy-portal
  namespace: clawbuddy-system
  labels:
    app: clawbuddy-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clawbuddy-portal
  template:
    metadata:
      labels:
        app: clawbuddy-portal
    spec:
      imagePullSecrets:
        - name: cr-pull-secret
      containers:
        - name: clawbuddy-portal
          image: <YOUR_REGISTRY>/<YOUR_NAMESPACE>/clawbuddy-portal:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: clawbuddy-portal
  namespace: clawbuddy-system
  labels:
    app: clawbuddy-portal
spec:
  type: ClusterIP
  selector:
    app: clawbuddy-portal
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
